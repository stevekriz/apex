// const request = require('supertest');
// const app = require('../server/app.js');

// describe('Test the /api/similar_listings/:primary_listing_id path', () => {
//   it('it should respond to a proper GET request', (done) => {
//     request(app)
//       .get(`/api/similar_listings/1`)
//       .expect(200, done);
//   });

//   it('it should respond to an improper GET request', (done) => {
//     request(app)
//       .get('/api/similar_listings/id')
//       .expect(400, done);
//   });
// });
